package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Domino Boricua Colors
val BoricuaBlue = Color(0xFF0CB7F2) // Traditional Sky Blue of the Historic PR flag
val BoricuaBlueDark = Color(0xFF0284C7) // Deeper sky blue
val BoricuaBlueLight = Color(0xFFE0F2FE) // Soft sky-blue mist

val BoricuaRed = Color(0xFFEF4444) // Vibrant PR Red
val BoricuaRedDark = Color(0xFFDC2626) // Deep Red

val WoodBrown = Color(0xFF78350F) // Mahogany Wood Accent
val WoodLight = Color(0xFFFEF3C7) // Warm Wood Background

val DominoSlate = Color(0xFF0B132B) // Deep Midnight Indigo (beautiful night theme)
val DominoSlateLight = Color(0xFF1C2541) // Card Navy/Slate
val DominoMint = Color(0xFF10B981) // Victory Green

// M3 Light Scheme Defaults
val PrimaryLight = BoricuaBlue
val SecondaryLight = BoricuaRed
val BackgroundLight = Color(0xFFF0F9FF) // Sophisticated sky blue-tinted background
val SurfaceLight = Color(0xFFFFFFFF)

// M3 Dark Scheme Defaults
val PrimaryDark = Color(0xFF38BDF8) // Soft luminous sky blue
val SecondaryDark = Color(0xFFF87171) // Pastel rose/red highlight
val BackgroundDark = DominoSlate
val SurfaceDark = DominoSlateLight
